# gitleaks fingerprint allowlist for the lark-ai-agent-bridge repo.
#
# Each line is a gitleaks fingerprint (as printed by `gitleaks detect`) that we
# have verified to be a false positive. Add one line per exemption together
# with the reason so future readers can re-audit.

# docs/superpowers/plans/2026-07-21-append-full-card-inline-timeline.md:451
# Tests the summary redaction logic with a hard-coded fake token literal
# ("sk-<REDACTED>"); the plan intentionally shows both the input
# and the redacted output, so the leak is illustrative, not a live secret.
0950995f3b67773e6fdd94702d1c209761109d80:docs/superpowers/plans/2026-07-21-append-full-card-inline-timeline.md:generic-api-key:451
