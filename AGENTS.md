# AI Agent 协作规范

## 需求讨论流程

收到新需求时，必须先完成需求确认再进入开发，不得跳过；该规则不区分需求大小。

流程：确认需求 → 完善和挖掘潜在需求 → 与用户确认 → 如有未确认的疑问，必须追问直到所有需求清晰、所有疑问解答。

讨论格式（按大类 + 关键需求点逐个列出）：

- [已明确] 用户已确认的需求点
- [潜在需求] 发现的潜在需求或遗漏点，用户确认后转为 [已明确]
- [裁撤] 已明确不做的需求
- [疑点] 待用户确认的疑问，用户解答后转为 [已明确]

每轮讨论后，将用户已确认的 [潜在需求] 和 [疑点] 更新为 [已明确]，保持状态实时准确。

所有疑点解答后，由用户显式确认“开始开发”方可进入实现阶段。确认后先更新 `tasks.md`，再开始编码，开发完成后必须完成一次 review 确认实现符合预期；如需修复需要完成后再次 review，如此类推

## 任务管理机制

- 根目录 `tasks.md` 用于持续跟踪项目需求与实现状态, 如果不存在，则创建一个
- 采用复选框格式： `[ ]` 未完成
- 按功能模块大类组织任务，每个大类下列出子任务
- 新需求处理流程：
  - 先在 `tasks.md` 中找到对应大类，在其下新增子任务
  - 若无对应大类，先创建新大类再添加子任务
- 完成任务后将任务直接清理掉
- 新需求进入实现前，优先更新 `tasks.md`
- 完成实现后，优先回写 `tasks.md`，保持任务状态与代码状态一致
- 不实现新功能、只做规划或收口时，也要同步更新 `tasks.md`
- 任何新增功能、文档、接入、脚本、配置项，都应在 `tasks.md` 中有对应记录
- 已完成的任务直接从 `tasks.md` 删除，不归档；`tasks.md` 仅保留进行中和待完成的任务

## 执行约束

- 本仓库如涉及网络相关的操作，默认在非沙箱环境执行，当前视为已授权
- 开发过程中，如涉及敏感信息，如密钥、token、secrect 等，不读取到上下文，不打印到终端，不存储到日志文档，全部脱敏处理

## 项目架构文档

- 面向用户的 AI Agent 协作提示词示例，见 [README.md](README.md)。`README.md` 只沉淀用户如何描述需求和触发响应 workflow 的提示词，具体如何处理由相关的工作流文档负责
- 项目当前功能架构与模块说明，按类别存放到 docs/framework 目录下分层分模块存放
- 特定的工作流程说明文档，按类别存放到 docs/workflow 目录下分层分模块存放

## 文档维护

- 方案、架构、功能、测试、流程等发生变化的时候，必须同步更新响应的文档

## 沟通风格

- 问题交流过程避免使用表格，用纯文档方式罗列
