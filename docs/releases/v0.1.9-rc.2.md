# v0.1.9-rc.2

## Breaking Changes

- 无。

## Features

- 配置面板整体打磨：所有 section 默认折叠、面板头填满卡片宽度，形成一致的卡片布局；从群里打开全局配置时，多出一个直达按钮去编辑该群的局部覆盖，私聊卡片行为不变。

## Bug Fixes

- 群覆盖直达按钮在真实卡片里不可见的紧跟修复。独立 CardKit 按钮改用 `element_id` 而非 `name`（`name` 只对 form 内控件有效），保留其 `local_config.edit` 回调。

## Upgrade Notes

- 无需迁移。

## Miscellaneous

- `tasks.md` 清理已完成的 Codex 卡片相关待办条目（纯文档，不影响运行时）。
