# v0.1.15-rc.1

## Breaking Changes

- 无。

## Features

- 支持通过 `/config set completion_status_text` 自定义任务完成文案。

## Bug Fixes

- 流式执行后续推理或工具时，持续保留最近一条完整回答。
- 修复富文本消息中 `@bot` 被误判为隐式 mention 的问题。
- 兼容 Codex 缺少旧 terminal event 的正常退出，不再误报失败。

## Upgrade Notes

- 无。

## Miscellaneous

- 引用 prompt 仅保留发送者 `open_id` 与原文，减少短指令误解。
