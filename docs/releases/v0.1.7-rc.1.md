# v0.1.7-rc.1

## Breaking Changes

- 无。

## Features

- 无。

## Bug Fixes

- 修复全局 Agent 切换为 Codex 后，Claude 专属命令（例如 `/.todo`）错误复用 Codex binary 与 home preset，进而把 Claude 的 `--output-format stream-json` 参数传给 Codex 并导致任务启动失败的问题。显式运行类型现在始终决定对应的 binary 与 home preset，Codex 与 Claude 可以稳定并存和切换。

## Upgrade Notes

- 无需迁移配置。升级后保留现有 Codex、Claude preset 与会话配置即可。

## Miscellaneous

- 无。
