# v0.1.4-rc.12

## Breaking Changes

- 无。

## Features

- AI 回复卡片底部的运行信息（meta）两行整体隐藏，不再向聊天暴露：第一行的 agent 类型 + 会话 ID、实际模型（含 effort）、上下文占用 / token 用量，以及第二行的用户 / IP / 工作目录。`MetaRows` 现恒返回空串，`buildMetaElements` 连同分隔线一起跳过，markdown 回复的 footer 同样为空。`ModelInfo` 仍在事件上追踪、`model_requested_actual_mismatch` audit 仍照常记录，仅渲染被抑制。

## Bug Fixes

- 无。

## Upgrade Notes

- 升级后回复卡片不再显示任何运行元信息行；如需排查模型 / 上下文占用，改看 audit 或 `/status`。

## Miscellaneous

- 无。
