# v0.1.13-rc.1

## Breaking Changes

- 无。

## Features

- 无。

## Bug Fixes

- 修复 `/config` 中状态栏元信息行开关未能可靠保存的问题。三个行开关现在分别使用明确的开启/关闭选项，关闭其中一行会持久化为 `false`。
- 兼容已打开的旧版配置卡片：仍可解析 CardKit 多选控件返回的对象形式选项值。
- 修复运行中卡片可能展示同一会话上一轮模型或上下文数据的问题；当前轮数据就绪前会显示同步状态。

## Upgrade Notes

- 无需迁移。重新打开 `/config` 即可使用新的状态栏元信息行开关。

## Miscellaneous

- 无。
