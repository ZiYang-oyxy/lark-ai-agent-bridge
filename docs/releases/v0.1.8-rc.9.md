# v0.1.8-rc.9

## Breaking Changes

- 无。

## Features

- 无。

## Bug Fixes

- **修复 Bash 等工具输出过长时，飞书卡片只剩输出、看不到执行命令的问题。** 卡片超过容量上限后，旧逻辑会从工具段头部截断，先丢掉工具名和命令围栏。现在单次工具输出最多保留 6000 字符，按约 60% 头部、40% 尾部保留并显式标记中间省略；工具名和命令始终完整展示。

## Upgrade Notes

- 无需迁移。改动仅影响工具调用区的卡片展示；未超限输出保持原样。

## Miscellaneous

- L1 全量 `go test ./...` 和 `scripts/verify.sh` 全绿；新增大输出回归测试，覆盖命令完整、输出有界、头尾保留和显示顺序。L3 已在 Test bot 通过真实飞书 `grep` 大输出验证，卡片可见命令、输出及省略提示。
