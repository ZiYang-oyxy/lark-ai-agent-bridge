# v0.1.4-rc.2

## Breaking Changes

- 无。

## Features

- 新增开发者模式与预发布(rc)升级通道

## Bug Fixes

- 无。

## Upgrade Notes

- 开发者模式默认关闭,行为与之前一致(仅跟踪 stable 通道)。管理员发送隐藏命令 `/.devel 1` 可开启预发布(rc)升级通道、`/.devel 0` 关闭;开启后需配置 `LAB_UPDATE_PRERELEASE_MANIFEST_URL` 指向独立的预发布 manifest 才会实际拉到 rc 版本。

## Miscellaneous

- 无。
