# v0.1.4-rc.3

## Breaking Changes

- 无。

## Features

- 无。

## Bug Fixes

- rc 通道 URL 自动推导 + rc 版本视为可自升级

## Upgrade Notes

- 开发者模式现即开即用:管理员发 `/.devel 1` 后,预发布通道 URL 由 stable manifest 自动推导(`-stable-`→`-prerelease-`),无需再配置 `LAB_UPDATE_PRERELEASE_MANIFEST_URL`(该变量仍保留为可选覆盖)。rc 版本现被视为可自升级的正式发布物,跑着 rc 的实例可继续升级到后续 rc。

## Miscellaneous

- 无。
