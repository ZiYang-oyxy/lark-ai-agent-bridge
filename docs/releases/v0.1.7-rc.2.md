# v0.1.7-rc.2

## Breaking Changes

- 无。

## Features

- 无。

## Bug Fixes

- 修复启用回复卡片元信息行后，Codex 第一排缺少模型的问题。Bridge 现在读取 Codex context-usage sidecar 已导出的真实模型，并在 runner 协议未上报模型时显示，例如 `⚙️ 019f93 · 🧠 gpt-5.6-sol · 🟢 ctx: 47%`。
- sidecar 模型回填严格限定为 Codex；Claude 继续仅使用 runner 返回的实际模型，有模型和无模型两种行为均保持不变。

## Upgrade Notes

- 无需迁移配置。仅在已通过 `/config` 或 `/local-config` 开启「元信息行」时可见该变化。

## Miscellaneous

- 无。
