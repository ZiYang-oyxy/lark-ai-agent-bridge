# v0.1.9-rc.1

## Breaking Changes

- 无。

## Features

- `tag` 与 `publish` 可复用精确绑定最终 commit、源码 tree、测试契约和 Go 工具链的 L1 测试凭证，避免同一发布重复执行全量测试；脏 worktree 或任一指纹变化都会自动重跑。

## Bug Fixes

- 工具链指纹覆盖 `GOTOOLCHAIN` 实际选择的 Go binary、compiler 和 linker SHA256，不再只校验可能属于旧版本的 launcher。

## Upgrade Notes

- 无。

## Miscellaneous

- 无。
